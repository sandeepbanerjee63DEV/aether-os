import { ModulePage } from "@/components/shared/module-page";
export default function Page() {
  return <ModulePage title="Workflow Builder" subtitle="Visual automation designer with triggers and actions." features={["Drag-Drop Builder", "Trigger Library", "Action Blocks", "AI Conditions", "Test Mode", "Version History"]} />;
}
